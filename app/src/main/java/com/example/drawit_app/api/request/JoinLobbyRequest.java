package com.example.drawit_app.api.request;

/**
 * Request model for joining an existing lobby
 */
public class JoinLobbyRequest {
    // Empty request, user ID will be derived from auth token
    // Can be extended later if additional parameters are needed
}
